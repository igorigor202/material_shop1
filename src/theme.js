import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#8A54E3',
      light: 'skyblue',
    },
    secondary: {
      main: '#15c630',
    },
    otherColor: {
      main: '#999',
    },
  },
});
